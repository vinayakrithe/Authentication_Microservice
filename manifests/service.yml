apiVersion: v1
kind: Service
metadata:
    name: vinayakritheauthenticationmicroservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: vinayakritheauthenticationmicroservice